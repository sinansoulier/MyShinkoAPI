name: Page Fetching

on:
  push:
    branches:
      - develop
      - main

jobs:
  test:
    runs-on: "ubuntu-latest"
    steps:
      - uses: actions/checkout@v2

      - name: Test
        run: echo "Hello Test"